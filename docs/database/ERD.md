# Diagrama Entidade-Relacionamento — LumiLivre

> Versão: 2026-07-01 — Baseline V1..V7 + materialized views V3.
> Fonte de verdade: migrations Flyway em `src/main/resources/db/migration/`.

## Visão geral

O schema cobre três núcleos de negócio:

1. **Acervo** — `book`, `book_copy`, `book_genre`, `dewey_classification`, `genre`.
2. **Pessoas & autenticação** — `reader`, `app_user`, `password_reset_token`,
   `course`, `academic_module`, `study_shift`.
3. **Operações** — `loan`, `loan_request`, `reservation`, `thesis`,
   `outbox_event`, `audit_log`.

Além deles, `library_settings` (single-row) guarda a configuração global do
tipo de biblioteca (`SCHOOL`/`STANDARD`), que condiciona a obrigatoriedade dos
campos acadêmicos do `reader`.

Tabelas de referência (`course`, `academic_module`, `study_shift`, `genre`,
`dewey_classification`) usam `INTEGER GENERATED BY DEFAULT AS IDENTITY` ou
chave natural (CDD); demais entidades de domínio usam `UUID` com
`gen_random_uuid()` (extensão `pgcrypto`).

## Diagrama principal

```mermaid
erDiagram
    course ||--o{ reader : "1:N"
    academic_module ||--o{ reader : "1:N"
    study_shift ||--o{ reader : "1:N"
    course ||--o{ thesis : "1:N"

    reader ||--o| app_user : "0..1:1"
    app_user ||--o{ password_reset_token : "1:N"

    book ||--o{ book_copy : "1:N"
    book ||--o{ book_genre : "1:N"
    genre ||--o{ book_genre : "1:N"
    dewey_classification ||--o{ book : "1:N"

    reader ||--o{ loan : "1:N"
    book_copy ||--o{ loan : "1:N"

    reader ||--o{ loan_request : "1:N"
    book_copy ||--o{ loan_request : "1:N"

    reader ||--o{ reservation : "1:N"
    book ||--o{ reservation : "1:N"

    course {
      int id PK
      varchar name UK
    }
    academic_module {
      int id PK
      varchar name UK
    }
    study_shift {
      int id PK
      varchar name UK
    }
    genre {
      int id PK
      varchar name UK
    }
    dewey_classification {
      varchar code PK
      varchar description
    }
    reader {
      uuid id PK
      varchar registration_number UK
      varchar full_name
      varchar cpf "UNIQUE quando NOT NULL"
      citext email "UNIQUE quando NOT NULL"
      int course_id FK "nullable; exigido em SCHOOL"
      int academic_module_id FK "nullable; exigido em SCHOOL"
      int study_shift_id FK "nullable; exigido em SCHOOL"
      varchar reader_category "modo STANDARD"
      varchar penalty_code
      timestamptz penalty_expires_at
      timestamptz deleted_at "soft delete"
    }
    library_settings {
      boolean id PK "singleton (TRUE)"
      varchar library_type "SCHOOL/STANDARD"
      timestamptz updated_at
    }
    app_user {
      uuid id PK
      citext email UK
      varchar password_hash
      varchar role "ADMIN/LIBRARIAN/READER"
      uuid reader_id FK "UNIQUE"
      varchar preferred_locale "pt-BR/en-US"
    }
    book {
      uuid id PK
      varchar isbn "UNIQUE parcial"
      varchar title
      varchar publisher
      varchar age_rating
      varchar dewey_code FK
      double rating "0..5"
      timestamptz deleted_at
    }
    book_copy {
      uuid id PK
      varchar copy_code UK
      varchar status "AVAILABLE/BORROWED/UNAVAILABLE/MAINTENANCE"
      uuid book_id FK
      varchar shelf_location
    }
    book_genre {
      uuid book_id FK,PK
      int genre_id FK,PK
    }
    loan {
      uuid id PK
      timestamptz borrowed_at
      timestamptz due_at
      timestamptz returned_at
      varchar status "ACTIVE/COMPLETED/OVERDUE"
      uuid reader_id FK
      uuid book_copy_id FK
      int renewal_count
      varchar penalty_code
    }
    loan_request {
      uuid id PK
      uuid reader_id FK
      uuid book_copy_id FK
      varchar status "PENDING/ACCEPTED/REJECTED/CANCELLED"
      varchar note
    }
    reservation {
      uuid id PK
      uuid reader_id FK
      uuid book_id FK
      varchar status "WAITING/READY/CANCELLED/EXPIRED/FULFILLED"
      int queue_position
      timestamptz expires_at
      timestamptz notified_at
    }
    thesis {
      uuid id PK
      varchar title
      varchar authors
      int course_id FK
      int completion_year
      varchar pdf_url
      bool is_active
    }
    password_reset_token {
      bigint id PK
      varchar token UK
      uuid app_user_id FK,UK
      timestamptz expires_at
    }
```

## Infraestrutura assíncrona

```mermaid
erDiagram
    outbox_event {
      bigint id PK
      varchar event_type "LOAN_CREATED/LOAN_RETURNED/REQUEST_*"
      varchar recipient_email
      varchar subject
      text body
      varchar status "PENDING/SENT/FAILED/DEAD_LETTER"
      int retry_count
      timestamptz processed_at
      timestamptz next_retry_at
    }
    audit_log {
      bigint id PK
      varchar actor
      varchar actor_role
      varchar target_id
      varchar action
      varchar result "SUCCESS/FAILURE/DENIED"
      text error_message
      timestamptz occurred_at
    }
```

Sem chave estrangeira para o domínio: `outbox_event` e `audit_log` são tabelas
append-only de infraestrutura (`BIGINT IDENTITY`, ADR-001).

## Views materializadas (V3)

```mermaid
flowchart LR
    loan -->|FILTER status| mv_dashboard_stats
    loan_request -->|status=PENDING| mv_dashboard_stats
    reservation -->|status=WAITING| mv_dashboard_stats

    book -->|JOIN book_copy JOIN loan| mv_top_books
    book_copy --> mv_top_books

    loan -->|borrowed_at >= now - 12 months| mv_loans_by_month
```

- `mv_dashboard_stats` — 1 linha com contadores agregados (`UNIQUE INDEX
  ((1))` mas refresh **não-concurrent** por limitação da expressão).
- `mv_top_books` — top 20 livros mais emprestados.
- `mv_loans_by_month` — volume mensal dos últimos 12 meses.

Refresh: `DashboardService.refreshViews()` a cada 15 minutos quando o JDBC
estiver em PostgreSQL (caso contrário, no-op — útil para suíte H2 de testes).

## Convenções

- **Soft delete** via `deleted_at TIMESTAMPTZ` em `reader`, `book`,
  `book_copy`, `thesis`, `app_user`. Índices `UNIQUE` partial usam
  `WHERE ... AND deleted_at IS NULL`.
- **Auditoria temporal** via `created_at`/`updated_at TIMESTAMPTZ NOT NULL
  DEFAULT now()` + trigger `touch_updated_at` em todas as tabelas mutáveis.
- **Email case-insensitive** via extensão `citext` (`reader.email`,
  `app_user.email`).
- **Locale por usuário** via `app_user.preferred_locale` (`pt-BR` por padrão);
  consumido pelo `MessageResolver` para resolver textos de e-mail.
- **Row Level Security deny-by-default** habilitado em todas as tabelas
  (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY` + `FORCE`). Backend conecta
  como owner; PostgREST / Supabase Data API ficam bloqueadas.

## Referências cruzadas

- DDL completo: [`ddl_standalone.sql`](./ddl_standalone.sql)
- Dicionário: [`data_dictionary.md`](./data_dictionary.md)
- Migração legado→LumiLivre: [`migration_from_legacy.md`](./migration_from_legacy.md)
- Portabilidade para outros bancos: [`portability_notes.md`](./portability_notes.md)
- ADRs relevantes: ADR-001 (chaves), ADR-004 (Supabase pooler), ADR-005 (RLS),
