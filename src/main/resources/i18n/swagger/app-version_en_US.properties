swagger.tag.app-version.name=21. App Version
swagger.tag.app-version.description=App version control (force update). The app queries it on startup; the admin edits it.
swagger.operation.appVersion.get.summary=Get version
swagger.operation.appVersion.get.description=Returns the latest and minimum supported version for the platform (public — called before login).
swagger.operation.appVersion.update.summary=Update version
swagger.operation.appVersion.update.description=Creates/updates the version data for a platform (ADMIN only).
